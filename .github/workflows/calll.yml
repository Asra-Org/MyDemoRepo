name: Testflow
on: 
  push:
    branches:
      - main
  workflow_call: 
jobs:
  Testworkflow:
    name: FumbleTest-flow
    runs-on: Ubuntu-latest
    steps:
      - name: Display Username
        run: echo " Username is Octocat "
